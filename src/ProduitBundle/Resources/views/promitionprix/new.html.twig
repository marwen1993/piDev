{% extends 'layoutback.html.twig' %}

{% block body %}
    <h1>Promitionprix creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Valider promotion" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('promitionprix_index') }}">Retourner vers la liste des promotion </a>
        </li>
    </ul>
{% endblock %}
