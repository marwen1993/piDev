{% extends 'layoutback.html.twig' %}

{% block body %}
    <h1>Modification de la promotion</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Valider modification" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('promitionprix_index') }}"><input type="submit" value="retourner vers la liste des promotions" />
        </li>

    </ul>
{% endblock %}
